JWT_SECRET=test-federation-secret
NAVIDROME_USERNAME=admin
NAVIDROME_PASSWORD=federation-nav-pw
POUTINE_INSTANCE_ID=poutine-a
POUTINE_OWNER_USERNAME=owner
POUTINE_OWNER_PASSWORD=federation-test-password
NAVIDROME_MUSIC_PATH=./test/federation/fixtures/music-a
POUTINE_HOST_PORT=3011
# Hubs in the federation test reach each other via docker DNS names that
# resolve to RFC1918 addresses; relax the production SSRF guard.
POUTINE_ALLOW_PRIVATE_PEER_URLS=1
